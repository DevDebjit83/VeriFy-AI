
# VeriFy AI Frontend

This is the frontend application for VeriFy AI - an AI-powered deepfake detection platform.  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  